export enum LogLevel {
    DEBUG = 'DEBUG',
    INFO = 'INFO',
    WARN = 'WARN',
    ERROR = 'ERROR',
}

export enum ContentType {
    IMAGE = 'image',
    LINK = 'link',
    PAGE = 'page',
    TEXT = 'text',
}
